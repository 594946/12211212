# 毛概刷题台

这是一个可直接发布到 GitHub Pages 的静态答题网站。

发布内容：

- `index.html`
- `styles.css`
- `app.js`

使用方式：

1. 新建 GitHub 仓库，例如 `maogai-quiz-site`。
2. 上传本目录下的文件到仓库根目录。
3. 在仓库 `Settings` -> `Pages` 中选择 `Deploy from a branch`，分支选 `main`，目录选 `/root`。
4. 等待 GitHub Pages 生成网址。

